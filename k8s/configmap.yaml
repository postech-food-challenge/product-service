apiVersion: v1
kind: ConfigMap
metadata:
  name: products-configmap
  namespace: tech-challenge
  labels:
    app: products
data:
  DB_HOST: "$DB_HOST"
  DB_PORT: "$DB_PORT"
  DB_USERNAME: "$DB_USERNAME"
  DB_PASSWORD: "$DB_PASSWORD"
